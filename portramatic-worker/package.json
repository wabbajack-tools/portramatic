{
  "private": true,
  "name": "portramatic-worker",
  "version": "1.0.0",
  "description": "Webworker for validating and catagorizing Portramatic images",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Timothy Baldridge (halgari)",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2",
    "@aws-sdk/client-rekognition": "^3.35.0",
    "@aws-sdk/client-connect": "^3.35.0"
  }
}
